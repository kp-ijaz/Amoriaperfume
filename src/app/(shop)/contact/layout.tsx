import { contactMetadata } from '@/lib/seo/static-pages';

export const metadata = contactMetadata;

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
