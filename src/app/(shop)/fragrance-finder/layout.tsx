import { fragranceFinderMetadata } from '@/lib/seo/static-pages';

export const metadata = fragranceFinderMetadata;

export default function FragranceFinderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
