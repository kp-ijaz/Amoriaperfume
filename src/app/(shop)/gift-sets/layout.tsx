import { giftSetsListingMetadata } from '@/lib/seo/static-pages';

export const metadata = giftSetsListingMetadata;

export default function GiftSetsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
