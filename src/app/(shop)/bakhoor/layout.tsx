import { bakhoorMetadata } from '@/lib/seo/static-pages';

export const metadata = bakhoorMetadata;

export default function BakhoorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
