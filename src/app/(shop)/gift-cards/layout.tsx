import { giftCardsMetadata } from '@/lib/seo/static-pages';

export const metadata = giftCardsMetadata;

export default function GiftCardsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
