import { brandInspirationMetadata } from '@/lib/seo/static-pages';

export const metadata = brandInspirationMetadata;

export default function BrandInspirationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
